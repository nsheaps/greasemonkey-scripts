{
  "name": "@greasemonkey-scripts/template",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "oxlint",
    "test": "echo \"no tests yet - add real tests when copying this template\""
  },
  "devDependencies": {
    "@types/greasemonkey": "~4.0.7"
  }
}
